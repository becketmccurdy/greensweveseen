# ===== Public client-side (exposed to browser) =====
# Supabase project URL (Settings > API > Project URL)
NEXT_PUBLIC_SUPABASE_URL=
# Supabase anon public key (Settings > API > Project API keys > anon)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Full origin of the deployed app, INCLUDING scheme (https://)
# e.g. https://app.greensweveseen.com
NEXT_PUBLIC_APP_URL=
# Mapbox token (optional features)
NEXT_PUBLIC_MAPBOX_TOKEN=

# ===== Server-side (never exposed) =====
# These are convenient server aliases; we also fall back to the NEXT_PUBLIC_* values.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Prisma/Supabase Postgres connection strings
# DATABASE_URL should be the Supabase POOLER (pgbouncer) URI, typically port 6543
# Supabase Dashboard: Project > Database > Connection string > URI (pooler)
DATABASE_URL=
# DIRECT_URL should be the direct 5432 URI (used by Prisma CLI/migrations)
DIRECT_URL=

# Optional: 3rd-party API
GOLF_COURSE_API_KEY=

